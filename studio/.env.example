# Copy to `.env` and fill in after you create the Sanity project (see /SETUP.md).
# These are read by sanity.config.ts / sanity.cli.ts. The SANITY_STUDIO_ prefix
# is required for values to be exposed to the Studio build.
SANITY_STUDIO_PROJECT_ID=your_project_id
SANITY_STUDIO_DATASET=production
